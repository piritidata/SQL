#In this SQL set I'm creating queries to quantify statistics about data pertaining to customers & order information. The database is a sample dataset.

-- Which products were sold in February at 548 Lincoln St, Seattle, WA 98101 and how many of each were sold and what was the total revenue?
SELECT SUM(Quantity), product, sum(quantity)*price AS revenue
FROM BIT_DB.FebSales
WHERE location="548 Lincoln St, Seattle, WA 98101"
GROUP BY product;

-- How many customers ordered more than 2 products at a time in February and what was the average amount spent by these customers?
SELECT count(distinct cust.acctnum), avg(quantity*price)
FROM BIT_DB.FebSales AS Feb
LEFT JOIN BIT_DB.customers AS cust
ON Feb.orderid=cust.order_id
WHERE Feb.Quantity>2
AND length(orderid)=6
AND orderid <>"Order ID";

--What is the total revenue for each product sold in January?
SELECT SUM(quantity)*price, product
FROM BIT_DB.JanSales
GROUP BY product;

--Which product was the cheapest one sold in January, and what was the price?
SELECT DISTINCT Product, price
FROM BIT_DB.JanSales
ORDER BY price ASC LIMIT 1;

-- Select the customer account numbers for all the orders that were placed in February
SELECT acctnum
FROM BIT_DB.customers AS CUST
INNER JOIN BIT_DB.FebSales AS FEB
ON cust.order_id=FEB.orderid
WHERE length(orderid) = 6
AND orderid <> "Order ID";

-- How many order in January (unique) were for an iPhone?
SELECT COUNT(DISTINCT orderid)
FROM BIT_DB.JanSales
WHERE Product="iPhone"
AND length(orderid) = 6
AND orderid <> 'Order ID';

-- How many unique orders were placed in January?
SELECT COUNT(DISTINCT orderid)
FROM BIT_DB.JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID';

--Find a string for price that starts with 5
SELECT DISTINCT Product, Price
FROM BIT_DB.FebSales
WHERE Price LIKE '5%';

-- Find a price string ending in .99
SELECT DISTINCT Product, Price
FROM BIT_DB.FebSales
WHERE Price LIKE '%.99';

-- Find a product that contains the word batteries
SELECT DISTINCT Product
FROM BIT_DB.FebSales
WHERE Product LIKE '%Batteries%';

-- Find the location of orders placed on 02/18/19 01:35
SELECT location
FROM BIT_DB.FebSales
WHERE orderdate = '02/18/19 01:35';

-- Find the total quantity of orders placed on 02/18/19
SELECT SUM(quantity)
FROM BIT_DB.FebSales
WHERE orderdate like '02/18/19%';

-- Find the dates of orders placed between 02/13/19 00:00 and 02/18/19 00:00
SELECT orderdate
FROM BIT_DB.FebSales
WHERE orderdate between '02/13/19 00:00' AND '02/18/19 00:00';

-- Find all the employees that start with the letter T and end with the letter M
SELECT employee 
FROM employees 
WHERE employee_name LIKE 'T_m';

-- Which of the following lines of code meet the following criteria: Start with 14, Have a single character after 14 and end with 9

WHERE employee_ID LIKE '14_9';

-- Find the orderIDs from March Sales where the order ID has the characters 16 in the beginning, followed by one unknown character then a 0 and any number
WHERE orderID LIKE '16_0%';

-- Find the employees whose names contain a literal underscore character
SELECT employee
FROM employees
WHERE employee_name LIKE '%!_%'
ESCAPE '!';

-- Find the employee names that end in either a, n, or c
SELECT name
FROM employees
WHERE name LIKE '%a'
OR name LIKE '%n'
OR name LIKE '%c';

-- Find the employee names that end in either a, n, or c [MySQL dialect]
SELECT employee_name
FROM employees
WHERE employee_name LIKE '%[anc]';

-- Which product brought the most revenue in January and how much revenue in total?
SELECT product, SUM(quantity*price)
FROM BIT_DB.JanSales
GROUP BY product
ORDER BY sum(quantity*price) desc
LIMIT 1;

--Was given following results from May Sales, requested to reverse engineer a SQL query that returns these results (containing May Sale, locations and order ids)
SELECT location, orderID
FROM BIT_DB.MaySales
WHERE orderID LIKE "1943_5"
AND length(orderID) = 6
AND orderID <> "Order ID"
ORDER BY OrderID;

-- Find order ids from Georgia (GA) where the order has an average price greater than $1000. Only find orders from dates beginning with "04/1". Assume the dates are stored in the MM/DD/YY HH:MM format
SELECT orderID
FROM BIT_DB.AprSales
WHERE orderdate LIKE "04/01%"
AND location LIKE "%GA%"
AND length(orderid) = 6
AND orderid <> "Order ID"
GROUP BY orderID
HAVING AVG(price) > 1000;

-- What was the average amount spent per account in February?
SELECT avg(quantity*price)
FROM BIT_DB.FebSales AS FEB
LEFT JOIN BIT_DB.customers AS cust
ON FEB.orderid=cust.order_id
WHERE length(orderid) = 6
AND orderid <> "Order ID";

-- How many of each type of headphone were sold in February
SELECT sum(quantity), Product
FROM BIT_DB.FebSales
WHERE Product LIKE "%headphones%"
AND length(orderid)=6
AND orderid <>"Order ID"
GROUP BY Product;




